import { connect } from 'react-redux';
import { sortByName, sortByAvailability } from '../../actions';
import SortBar from '../../components/sort-bar/SortBar';
import

const sortResults = (cars, filter) => {}
